apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}-health
  labels:
     app: {{ .name }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: health
      protocol: TCP
      name:  health
  selector:
    app: {{ .name }}


